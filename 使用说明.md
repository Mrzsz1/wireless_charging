# 使用说明 · 无线充电调度 LLM Wiki

> 本文说明：**之后怎么做、怎么用**本知识库。  
> 架构与决策见 `prd.md`；Agent 编译规程见 `AGENTS.md`。

可以把系统想成三条线：

1. **养库** — 投料 + A 编译（让 wiki 变厚）  
2. **问库** — `/solve` 找解法、`/novelty` 查是否做过  
3. **锻 idea** — B 阶段，显式触发且须你确认  

**Graphify** 是导航加速器；**正文真相在 `wiki/`**（不是 `graphify-out/`）。

---

## 0. 路径与入口

| 项 | 路径 / 文件 |
|----|-------------|
| Vault 根目录 | `E:\知识库\wireless_charging` |
| 人类入口 | `HOME.md` · `wiki/maps/map-home.md` |
| 内容目录 | `wiki/index.md` |
| 库有多新 | `wiki/maps/library-status.md` |
| 按论文关键词浏览 | `wiki/maps/map-domain-keywords.md` |
| 产品约定 | `prd.md` |
| 系统架构 | `ARCHITECTURE.md` |
| Agent 宪法 | `AGENTS.md` |

用 **Obsidian** 打开上述文件夹作为库即可。Windows 用户也可双击桌面的“无线充电 LLM Wiki”快捷方式；启动器优先使用 Obsidian，未安装时回退到系统默认 Markdown 程序。

重新生成桌面快捷方式：

```powershell
.\tools\create-wiki-shortcut.ps1
```

另有一个独立的 **“无线充电论文搜索”** 桌面按钮。它只有在双击时才执行搜索，与打开 Wiki 的快捷方式互不影响。重新生成：

```powershell
.\tools\create-paper-search-shortcut.ps1
```

---

## 1. 日常：有问题就问（主用法）

### 1.1 打开 Obsidian

- 库路径：`E:\知识库\wireless_charging`
- 建议从 **`HOME.md`** 或 **`wiki/maps/map-home`** 进入
- 看库水位：`wiki/maps/library-status`（问答时务必参考）

### 1.2 配置 Claudian（一次即可）

把下列文件中的「系统指令」复制到 Claudian 自定义提示 / 命令：

| 用途 | 模板文件 |
|------|----------|
| 找解法 | `schema/claudian-solve.md` |
| 查是否做过 / 新颖性（仅库内） | `schema/claudian-novelty.md` |

### 1.3 两种标准问法

| 你想做什么 | 怎么问 | 你应看到的答案形态 |
|------------|--------|--------------------|
| 有问题，找可用或可迁移解法 | `/solve` + 场景、约束、目标 | 直接可用 / 可改可用 / 库内未见 + `[[wikilink]]` + **库水位** |
| 有 idea，看库里是否已被覆盖 | `/novelty` + idea 要点 | 已覆盖 / 部分重叠 / 未见 + 证据链接 + **库水位**（**不是**全球查新） |

**`/solve` 示例：**

```text
/solve
多静态定向充电器、传感器会发在线充电请求，且存在波干涉时，
有没有可用的朝向/功率分布控制方法？希望最大化充电效用。
```

**`/novelty` 示例：**

```text
/novelty
在并发 RF 充电下，用在线算法同时做充电器开关调度和 AoI 优化——
库里有没有已经做完的工作？
```

### 1.4 不靠对话时：自己浏览 wiki

| 目的 | 去哪 |
|------|------|
| 总览 | `wiki/index.md`、`wiki/maps/*` |
| 单篇文献卡 | `wiki/sources/src-*.md` |
| 方法与对照 | `wiki/methods/`、`wiki/syntheses/` |
| 关系图 | `graphify-out/graph.html`，或终端 `graphify query "..."` |

---

## 2. 加新论文：养库流程（复利）

```text
自动搜索 → raw/inbox/auto-discovered/runs/search-*/
手动投放 → raw/inbox/manual-drop/
  → 人工 triage（自动候选用 paper-triage.ps1）
  → selected / 确认项进入 MinerU，输出到 raw/canonical/<论文名>/ 并保留来源
  → MinerU：生成 .md + images/（可保留 .pdf / 可选 .html 仅人读）
  → Codex 或 Grok：「按 AGENTS.md / schema/agent-a-compile.md 做 A 编译」
  → 更新图：Codex 用 $graphify . --update（或 Grok 按 graphify skill）
  → 用 /solve 或 /novelty 验证库是否更好用
```

### 2.1 一文一夹示例

```text
raw/canonical/
  SomePaper2023/
    SomePaper2023.pdf
    full.md                 # 或与 PDF 同名的 .md（A 编译主输入）
    images/                 # MinerU 图片，勿与其他论文合并
    SomePaper2023.html      # 可选，仅浏览器人读
```

### 2.2 硬规则（不要破）

- 只收**论文 / 专利 / 预印本**等正式文献；**禁止**网页、blog、PPT  
- A 编译主源是 **Markdown**，不是 HTML  
- 多篇论文**不要**共用一个全局 `images/`  
- `problem` / `idea` **不要**让 AI 自动落盘；写入前必须你确认  
- 通用外搜与 `/novelty` 实时查新必须**先问你**；已配置 Paper Search 可自动写 inbox，但不得直接写成 wiki 硬事实  
- `manual_upload` / `auto_discovery` 是永久来源字段，移动到 canonical 后也不能删  
- 不要用 Graphify 的 `--wiki` **覆盖**本库 `wiki/` 目录结构  

### 2.3 编译成功后你应看到

- `wiki/sources/` 新增 `src-*.md`  
- `wiki/index.md`、`wiki/maps/library-status.md` 更新  
- `logs/log.md` 追加一条 `ingest`  
- （建议）`graphify-out/` 已 `--update`  

---

## 3. 锻 idea（B 阶段，按需）

1. 先看综合页里的 **Gaps**（如 `wiki/syntheses/`），或自有假说  
2. 对 Agent / Claudian 说：「根据 gap 起草 idea，**先不要写入文件**，给我审」  
3. 你确认后，再写入 `wiki/ideas/`（`user_confirmed: true`，并链回 source / synthesis）  
4. 再用 `/novelty` 在库内压一遍重叠  

文献页（A 类）**禁止**写「我们将贡献…」；贡献性表述只进 `problem` / `idea`。

当前已正式化的第一个问题是 `wiki/problems/prob-joint-deployment-online-interference.md`。它只定义研究问题和证据边界；算法骨架尚未作为 idea 落盘。

---

## 4. 维护（偶尔）

| 频率 | 做什么 |
|------|--------|
| 每批 A 编译后 | `$graphify . --update`（或等价 skill） |
| 链接乱、孤儿页、前后矛盾感强 | 让 Agent 按 `schema/lint-checklist.md` 做 Lint |
| 受控词表缺词 | 写入 `schema/vocab-proposals.md` → 你确认 → 再合并 `schema/vocab.yaml` |
| 新论文带 Keywords / Index Terms | 写入 source 的 `paper_keywords`，补 `map-domain-keywords`；运行 `py -3 tools/domain_keywords.py --check` |
| 需要库外查新 | 明确批准外搜；结果带日期；先不进 wiki 正文 |
| 核心 synthesis / 问答模板改动 | 运行 `py -3 tools/wiki_eval.py`，再抽答 10 条回归问题 |

---

## 5. 工具分工

| 工具 | 用来做什么 |
|------|------------|
| **Obsidian** | 浏览、点 `[[wikilink]]`、预览 md+images |
| **Claudian** | 日常 `/solve`、`/novelty` |
| **Codex CLI / Grok CLI** | A 编译、Lint、结构维护、触发 Graphify skill |
| **Graphify** | `query` / `path` / `explain`、查看 `graph.html`（派生索引） |
| **Paper Search** | 四源 → `auto-discovered/runs` 候选报告；不自动入库 |
| **Paper Triage** | 标记 selected/rejected，建立 `auto-discovered/papers` 已选队列 |
| **MinerU** | PDF → md + images |

论文自动发现：双击桌面的“无线充电论文搜索”，或运行 `.\tools\paper-search.ps1`；筛选示例：`.\tools\paper-triage.ps1 ".\raw\inbox\auto-discovered\runs\search-...\results.json" --select 1,3-5`。桌面按钮只在点击时搜索，不安装计划任务。完整参数和多 Key 配置见 `tools/README-paper-search.md`。

当前累计候选状态为：46 pending / 6 selected / 14 rejected / 12 promoted。12篇公开PDF已下载、由MinerU转成canonical Markdown并完成A编译；selected候选等待新的合法开放全文落点。promoted的12篇已成为wiki source。

论文关键词采用三层治理：source 保存作者原词，`wiki/maps/map-domain-keywords.md` 保存规范别名和证据链，只有匹配字段确实缺词时才写 `schema/vocab-proposals.md`。不要按词频直接修改 `vocab.yaml`。

MinerU API 批量自动化：`.\tools\mineru-to-md.ps1 ".\raw\inbox\manual-drop"` 或对自动发现已选目录运行；完整参数见 `tools/README-mineru-api.md`。脚本只生成 `pending_ingest` raw 条目并传播 provenance，不会绕过 A 编译直接写 wiki。

### Graphify 常用（图已建好时）

```powershell
cd E:\知识库\wireless_charging
graphify query "在线调度 波干涉 定向充电"
graphify path "A" "B"
graphify explain "fairness"
```

- **Codex** 建图/更新：`$graphify .` 或 `$graphify . --update`（注意是 **`$`**）  
- **Grok**：按 `.agents/skills/graphify/SKILL.md` 执行建图/更新  

---

## 6. 推荐使用节奏

### 刚上手（现在）

1. Obsidian 打开 `wiki/syntheses/` 下 3 篇综合页  
2. Claudian 配置 solve / novelty 模板  
3. 从 `evals/gold_questions.json` 选题运行 `/solve` / `/novelty`，答案保存到 `evals/answers/<case-id>.md`  
4. 运行 `py -3 tools/wiki_eval.py --answers-dir evals/answers` 检查 wikilink 与库水位契约  
5. 阅读首个正式 B 问题 `wiki/problems/prob-joint-deployment-online-interference.md`，再决定是否锁定硬件假设并孵化 idea  

### 之后每次读新文献

走 **§2 养库流程**，不要只堆 PDF 不编译。

### 开题 / 想 idea 时

走 **§3**，并始终用库水位限定「新颖」的含义。

---

## 7. 成功标准（你怎么判断「在用」）

系统算真正在用，当且仅当大致满足：

1. Claudian 能带着 **`[[wikilink]]`** 给出解法或重叠文献，而不是空泛聊天  
2. 答案会声明「基于当前知识库 N 篇 / 水位…」，**不装**全球查新  
3. 新论文进入后 **`wiki/` 变厚**，而不是只躺在 `raw/`  
4. idea 有来源锚点，且经过你确认  

---

## 8. 相关文件速查

| 文件 | 内容 |
|------|------|
| `prd.md` | 产品需求与锁定决策 |
| `ARCHITECTURE.md` | 分层、组件、数据契约、运行流程与当前漂移 |
| `AGENTS.md` | Agent 维护宪法 |
| `schema/agent-a-compile.md` | A 编译步骤 |
| `schema/ingest-checklist.md` | 投料检查清单 |
| `schema/lint-checklist.md` | 健康检查 |
| `schema/domain-keywords.md` | 论文原词、领域导航与受控词表的三层规则 |
| `schema/claudian-solve.md` | `/solve` 提示词 |
| `schema/claudian-novelty.md` | `/novelty` 提示词 |
| `schema/references/karpathy-llm-wiki.md` | Karpathy 范式摘要 |
| `schema/references/graphify.md` | Graphify 用法 |
| `raw/canonical/README.md` | 一文一夹与 MinerU 约定 |
| `tools/README-paper-search.md` | 四源论文自动发现、下载与定时运行 |
| `tools/domain_keywords.py` | 只读检查 source 关键词元数据与地图覆盖 |
| `evals/README.md` | 10 条问答回归用例与校验方法 |
| `logs/log.md` | 时间线 |

---

## 9. 一句话

**平时**：Obsidian + Claudian 问库。  
**找新论文**：Paper Search / manual-drop → inbox triage → MinerU 一文一夹并保留来源 → Agent A 编译 → Graphify 更新。  
**有 idea**：先起草审阅，你确认后再写入，再用 `/novelty` 压库。

## 核心专著（2026-08-01）

知识库现在包含两本核心参考书：`Algorithmic Game Theory`（775 页、29 章）和 `Approximation Algorithms`（396 页、30 章）。原始 PDF 保留在 `raw/inbox/manual-drop/`；按章节拆分的 Markdown 在 `raw/canonical/algorithmic-game-theory/chapters/` 与 `raw/canonical/approximation-algorithms/chapters/`。查询模型、算法、解决办法、近似比、均衡或机制时，先运行：

```powershell
py -3 tools/core_reference_search.py "<问题>" --limit 8
```

回答必须携带书名、章节和 PDF physical pages；质量门禁见 `raw/canonical/core-books-quality.json`。
