# Implement

1. 在 `AskView.tsx` 抽取唯一活动步骤索引、阶段标题和说明。
2. 重构处理消息 JSX，加入动态加载头、轨道、三点和流式光标。
3. 在 `AskView.css` 增加低成本动画、响应式布局和 reduced-motion 覆盖。
4. 更新前端静态契约测试，断言 active 唯一投影、动态组件和无障碍属性。
5. 更新组件规范，执行前端全测、Rust 回归与 Tauri release 编译。
