# 已选自动发现候选

`paper_triage.py` 为每个 `selected` 候选建立一文一夹的 `metadata.json`，使用 `--download-selected` 时另有 `paper.pdf`。

这里的文献仍是 inbox；确认晋升 canonical 并完成 MinerU 后，才可进入 A 编译。

2026-07-14：本目录有 **10 个候选元数据夹**；其中 **7 个有经签名与首页标题校验的 `paper.pdf`，并已晋升 canonical / 完成 MinerU**，另外 3 个只有受限出版落地页。所有条目均尚未完成 A 编译。
